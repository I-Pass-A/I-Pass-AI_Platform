# Database URL configuration (SQLite is default)
DATABASE_URL=sqlite:///./ipassa.db

# Google Gemini API key (Required for live AI features, fallback mock used otherwise)
GEMINI_API_KEY=

# Secret key for local JWT sign/verification
JWT_SECRET_KEY=supersecretkey_ipassa_123456789
